name: ruoqi_platform_api
description: "RuoQi platform domain API: DTOs and client, shared by app and pc."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  json_annotation: ^4.12.0
  ruoqi_network:
    path: ../ruoqi_network

dev_dependencies:
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  json_serializable: ^6.9.0
